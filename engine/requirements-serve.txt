# Minimal deps to serve the AI endpoints (/compose /coach /edit /variations).
# Intentionally excludes librosa/scipy/soundfile/pretty_midi so the hosted build is
# fast and dependency-light. Audio /extract needs those and is not served here.
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
anthropic>=0.40
numpy>=1.24
pydantic>=2
eval_type_backport; python_version < '3.10'
